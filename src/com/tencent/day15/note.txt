 	2018/7/28
第15天

一、GC(垃圾收集)
	GC全称: Gabage Collection,垃圾收集的意思
	
	JVM提供了一个守护进程，这个进程会自动检测对象是否超过了作用域，
	如果超出就会被认为此对象是垃圾对象，并且没有对象引用，会自动回收此对象的内存空间
	这种机制就是垃圾回收机制
	
	finalize()方法: 对象被回收之前的最后一个方法，由垃圾回收器调用此方法
	System.gc();	手动回收，主动通知垃圾回收机制回收对象
	
	JDK1.8版本接口可以写方法体
	